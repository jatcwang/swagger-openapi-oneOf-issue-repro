Minimized reproduction of issue with `oneOf` when using swagger-request-validator-pact

The pact and openapi file is in `src/main/resources`

To execute
```
mvn exec:java -Dexec.mainClass="Main" -e
```
